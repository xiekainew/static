{
  "name": "fis3-test",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "fis3 server start -d ./dist -p 9001 && fis3 release -wL",
    "test": "echo \"Error: no test specified\" && exit 1",
    "stop": "fis3 server stop && fis3 server clean"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "fis3-server-jello": "0.0.11",
    "node-sass": "^4.13.0"
  }
}
