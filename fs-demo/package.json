{
  "name": "fis3-test",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "rm -rf dist/ && fis3 release -d ./dist",
    "release": "rm -rf dist/ && fis3 release -wL",
    "dev": "cross-env NODE_ENV=development node server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "stop": "fis3 server stop && fis3 server clean"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "compression": "^1.7.4",
    "cross-env": "^6.0.3",
    "express": "^4.17.1",
    "fis-optimizer-clean-css": "0.0.12",
    "fis-optimizer-html-compress": "0.0.7",
    "fis-optimizer-png-compressor": "^0.2.2",
    "fis-optimizer-uglify-js": "^0.2.3",
    "fis-parser-babel-5.x": "^1.4.0",
    "fis-parser-jdists": "^2.2.3",
    "fis-parser-node-sass": "^1.0.6",
    "fis3-hook-relative": "^2.0.3",
    "fis3-postpackager-loader": "^2.1.11",
    "fis3-preprocessor-autoprefixer": "^0.1.1",
    "fis3-server-jello": "0.0.11",
    "glob": "^7.1.6",
    "http-proxy-middleware": "^0.20.0"
  }
}
